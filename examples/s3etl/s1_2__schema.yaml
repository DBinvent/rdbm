database:
  - schema:
    schemaName: test_schema
    owner: postgres
    tables:
      - table:
          tableName: test_table
          columns:
            - column:
                name: tr
                type: int
                constraint:
                  foreignKey:
                    references: test_ref_table
